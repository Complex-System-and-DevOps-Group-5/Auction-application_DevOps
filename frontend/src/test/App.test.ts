//import { render, screen } from "@testing-library/react";
import { describe, expect, it } from "vitest";
//import App from "../App.tsx";

describe('AUCTION', () => {
    it("should render", () => {
        //render(<App />);
        //expect(screen.getByLabelText("AUCTION")).toBeInTheDocument();
        expect(true).toBeTruthy
    });
});